# Mac App Store apps (installed via `mas`) — OPT-IN.
#
# App Store apps are NOT installed by default: company machines often restrict
# Apple-account sign-in / allowed apps. The bootstrap prompts yes/no per line
# (default: skip). Install manually with:  mas install <id>
#
# Format: <id>  # <name>
1276493162  # reMarkable desktop   (base app; App Store only)
1451685025  # WireGuard            (App Store only; no official cask)
