# Mac App Store apps (via `mas`) — OPT-IN, never installed by default (my work
# machine restricts Apple sign-in). Bootstrap prompts per line, default skip.
# Manual: mas install <id>
#
# <id>  # <name>
1276493162  # reMarkable desktop   (App Store only)
1451685025  # WireGuard            (App Store only; no official cask)
